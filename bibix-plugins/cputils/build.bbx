import bibix
from bibix.plugins() import ktjvm
import maven

// class path를 줬을 때, 거기 속한 클래스 이름의 목록, 그 클래스들 중에서 특정 클래스나 인터페이스를 상속 혹은 구현한 클래스들 찾기 기능 제공
// junit5와 scalatest와 같은 플러그인에서 유용할 것.
// rule을 제공하지는 않는다. 어쩌면 나중에 maven dep같은걸로 바꾸는게 나을까?
// 사실 이미 비슷한 일을 하는 라이브러리들이 있는 것 같긴 한데 왠지 쓰기 찝찝하게 생겨서..

lib = ktjvm.library(
  deps = [
    maven.dep("org.jetbrains.kotlin", "kotlin-stdlib-jdk8", "1.6.20"),
  ],
  srcs = glob("src/**/*.kt"),
)
